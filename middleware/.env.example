# ===== TOKKATOT MIDDLEWARE CONFIGURATION =====
# Copy this file to .env and update values for your environment

# Database
DB_HOST=DB_HOST_HERE
DB_PORT=DB_PORT_HERE
DB_USER=DB_USER_HERE
DB_PASSWORD=YOUR_POSTGRES_PASSWORD_HERE
DB_NAME=DB_NAME_HERE
DB_SSLMODE=disable  # "disable" for local, "require" for production

# Server info
SERVER_PORT=SERVER_PORT_HERE
SERVER_HOST=SERVER_HOST_HERE

# Security (Change JWT_SECRET in production!)
JWT_SECRET=CHANGE-ME-generate-with-openssl-rand-hex-32

# Deployment Mode (development or production)
ENVIRONMENT=development

# Initial Admin (Used by migration tools to seed the database)
INITIAL_ADMIN_EMAIL=INITIAL_ADMIN_EMAIL_HERE
INITIAL_ADMIN_PASSWORD=INITIAL_ADMIN_PASSWORD_HERE

# SSL/TLS (Relative to middleware/ directory)
TLS_CERT=../certs/cert.pem
TLS_KEY=../certs/key.pem

# Web Push (VAPID Keys - Generate these for production)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:admin@tokkatot.com
