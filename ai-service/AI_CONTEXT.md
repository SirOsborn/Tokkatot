# AI Context — AI Service (PyTorch / FastAPI)

> **Everything you need is in [`/AI_INSTRUCTIONS.md`](../AI_INSTRUCTIONS.md).**
>
> That file is the single source of truth for the entire Tokkatot project, including a dedicated
> `## 🤖 AI Service — PyTorch` section covering:
> - EfficientNetB0 + DenseNet121 ensemble architecture
> - FastAPI endpoints (/predict, /health, /classes)
> - inference.py ChickenDiseaseDetector pattern
> - Model file requirements (outputs/ensemble_model.pth — gitignored)
> - Docker deployment commands
> - Python environment setup
