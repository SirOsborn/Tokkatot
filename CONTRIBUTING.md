# Contributing to Tokkatot

Please read `AGENT.md` for project conventions, architecture, and workflow.

Key points:
- Keep frontend modular (one page = one stylesheet).
- Khmer-first UI text with translations.
- Use `.env` for configuration and secrets.

